/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['picsum.photos',"res.cloudinary.com", "firebasestorage.googleapis.com" ],
      },

};

// module.exports = nextConfig;
export default nextConfig;
